export interface Iproduct {
  id: number;
  name: string;
  price: number;
}
/// criei uma forma de dado onde posso manipular da forma que mais preciso. posso usar como um unico objeto ou transformar um array contendo varios dados desse mesmo tipo usando apenas o dado[]